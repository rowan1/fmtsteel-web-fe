export enum Routes {
    home = "/",
    admin = "/admin",
    login = "/login",
    projects = "/admin/projects",
    services = "/admin/services",
    clients = "/admin/clients",
    contacts = "/admin/contacts"
}